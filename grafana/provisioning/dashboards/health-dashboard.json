{
  "id": null,
  "uid": "coinflip-health",
  "title": "Coinflip Parade - Service Health",
  "tags": ["coinflip", "health"],
  "timezone": "browser",
  "schemaVersion": 39,
  "version": 1,
  "refresh": "10s",
  "panels": [
    {
      "type": "stat",
      "title": "Ingestion - Service Status",
      "datasource": "Prometheus",
      "targets": [
        {
          "expr": "up{service=\"ingestion\"}"
        }
      ],
      "gridPos": { "x": 0, "y": 0, "w": 4, "h": 4 },
      "colorMode": "value",
      "thresholds": {
        "mode": "absolute",
        "steps": [
          { "color": "red", "value": 0 },
          { "color": "green", "value": 1 }
        ]
      }
    },
    {
      "type": "stat",
      "title": "Persistence - Service Status",
      "datasource": "Prometheus",
      "targets": [
        {
          "expr": "up{service=\"persistence\"}"
        }
      ],
      "gridPos": { "x": 4, "y": 0, "w": 4, "h": 4 },
      "colorMode": "value",
      "thresholds": {
        "mode": "absolute",
        "steps": [
          { "color": "red", "value": 0 },
          { "color": "green", "value": 1 }
        ]
      }
    },
    {
      "type": "stat",
      "title": "Ingestion - Uptime (seconds)",
      "datasource": "Prometheus",
      "targets": [
        {
          "expr": "time() - process_start_time_seconds{service=\"ingestion\"}"
        }
      ],
      "gridPos": { "x": 8, "y": 0, "w": 4, "h": 4 }
    },
    {
      "type": "stat",
      "title": "Persistence - Uptime (seconds)",
      "datasource": "Prometheus",
      "targets": [
        {
          "expr": "time() - process_start_time_seconds{service=\"persistence\"}"
        }
      ],
      "gridPos": { "x": 12, "y": 0, "w": 4, "h": 4 }
    },
    {
      "type": "timeseries",
      "title": "Ingestion - Error Rate",
      "datasource": "Prometheus",
      "targets": [
        {
          "expr": "rate(request_errors_total{service=\"ingestion\"}[5m])"
        }
      ],
      "gridPos": { "x": 0, "y": 4, "w": 8, "h": 6 }
    },
    {
      "type": "timeseries",
      "title": "Persistence - Error Rate",
      "datasource": "Prometheus",
      "targets": [
        {
          "expr": "rate(request_errors_total{service=\"persistence\"}[5m])"
        }
      ],
      "gridPos": { "x": 8, "y": 4, "w": 8, "h": 6 }
    },
    {
      "type": "timeseries",
      "title": "Ingestion - Last Batch Duration (ms)",
      "datasource": "Prometheus",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(batch_duration_ms_bucket{service=\"ingestion\"}[5m])) by (le))"
        }
      ],
      "gridPos": { "x": 0, "y": 10, "w": 16, "h": 6 }
    },
    {
      "type": "timeseries",
      "title": "Persistence - Save Duration (ms)",
      "datasource": "Prometheus",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(save_duration_ms_bucket{service=\"persistence\"}[5m])) by (le))"
        }
      ],
      "gridPos": { "x": 0, "y": 16, "w": 16, "h": 6 }
    },
    {
      "type": "stat",
      "title": "Ingestion - Last Successful Batch",
      "datasource": "Prometheus",
      "targets": [
        {
          "expr": "max(last_success_timestamp_seconds{service=\"ingestion\"})"
        }
      ],
      "gridPos": { "x": 0, "y": 22, "w": 8, "h": 4 }
    },
    {
      "type": "stat",
      "title": "Persistence - Last Successful Save",
      "datasource": "Prometheus",
      "targets": [
        {
          "expr": "max(last_success_timestamp_seconds{service=\"persistence\"})"
        }
      ],
      "gridPos": { "x": 8, "y": 22, "w": 8, "h": 4 }
    }
  ]
}
